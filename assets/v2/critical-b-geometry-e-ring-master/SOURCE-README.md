# OWL-3 CRITICAL B-Geometry E-Ring Master Asset Kit

This kit promotes the OWL-3 CRITICAL candidate using B geometry and E halfway-salmon rings. It uses the CRITICAL human-gold-branch owl source, preserves the owl body and black feather linework, matches OWL-2 inner-ring alpha geometry, and recolors only semantic red ring layers.
