# OWL-3 CRITICAL Audit Note

Final doctrine: B geometry + E halfway-salmon rings.

- Do not globally filter this asset.
- Do not recolor the owl body; prior body recolor tests introduced speckled texture and risked feather-line loss.
- L1 inner ring geometry is copied from OWL-2 approved L1 alpha geometry and recolored to CRITICAL red RGB(226,92,96).
- L3 uses the CRITICAL human-gold-branch transparent source with B geometry bbox (236,233,851,858).
- Gold branch/leaves/olive are preserved from the human-selected source.
- Meander ring is the CRITICAL original source.
