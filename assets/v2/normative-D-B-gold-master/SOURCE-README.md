# NORMATIVE v2 Final D Geometry + B Parchment-Gold Master Asset

This package is the current selected NORMATIVE v2 master candidate.

## Decision

Carey selected **D geometry**: C alignment with the owl scaled to **94%**, then selected **B parchment-gold color** because it is the lightest candidate and keeps the owl crisp against the dark inner field.

## Layer names

1. `L1-inner-field`
2. `L2-meander-ring`
3. `L3-v2-approved-owl-D-geometry-B-parchment-gold`
4. `L4-outer-ring`
5. `COMPOSITE-preview`

## Wing-line audit

The B color update preserves the prior D owl alpha silhouette exactly. A dark-line pixel comparison and wing-end zoom proof are included in `proofs/` so the wing-end linework can be checked before promotion into the repository pipeline.

## TIFF note

`NORMATIVE-V2-D-B-GOLD-MASTER-ASSET-1080.tiff` is a multi-page TIFF containing component pages plus the final composite preview. It is not a proprietary Pixelmator/Photoshop editable-layer TIFF unless re-exported from those applications.

## Color management

The review files remain in sRGB. No CMYK conversion has been applied.
