# Owl Semaphore — Release Bundle Manifest
#
# Canonical list of files and directories that MUST appear inside the
# GitHub release bundle (`owl-semaphore-<TAG>.zip`) produced by
# `.github/workflows/release-assets.yml`.
#
# Format:
#   - One path per line, relative to the repository root.
#   - Lines beginning with `#` and blank lines are ignored.
#   - A trailing `/` marks a directory (copied recursively).
#
# This file is the single source of truth consumed by:
#   - `.github/workflows/release-assets.yml` to validate and assemble the bundle.
#   - `tests/test_release_bundle.py` to verify in CI that the workflow
#     copies every listed entry into the bundle and that every listed
#     entry exists at the repo root.
#
# To add a new canonical release artifact: add it here. Do not edit the
# workflow's file list directly — the workflow reads this manifest.

# License + provenance
LICENSE
INTEGRITY-MANIFEST.md
RELEASE-HASHES.txt
VALIDATION-SCRIPT-SPEC.md

# Citation + Zenodo metadata
CITATION.cff
.zenodo.json

# Human-readable project docs
README.md
CHANGELOG.md

# Canonical specification source (markdown)
OWL-SEMAPHORE-SYSTEM.md
OWL-SEMAPHORE-EXPLANATION.md
OWL-1-NORMATIVE.md
OWL-2-NON-NORMATIVE.md
OWL-3-CRITICAL.md
OWL-4-METACOGNITIVE.md

# Canonical specification PDFs (generated, byte-archived in the release)
OWL-SEMAPHORE-SYSTEM.pdf
OWL-SEMAPHORE-EXPLANATION.pdf
OWL-1-NORMATIVE.pdf
OWL-2-NON-NORMATIVE.pdf
OWL-3-CRITICAL.pdf
OWL-4-METACOGNITIVE.pdf

# Asset tree (masters, layers, exports, releases)
assets/
